defdir ( hello ) # This is an comment
{
	.*:inlcude
	c.h:yes
}

context {
	defdir (yes){
		$(hello) : include
		${which} : bin
	}
	undefdir (hello)
	raw  :whatsup
	where
	make{
		anything,
		here.
	}
	context {
		dosomething
		here
		defdir (no){
			hhh : yyy
		}
	}
}
