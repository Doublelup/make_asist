defdir ( hello ) # This is an comment
{
	.*:inlcude
	c.h:yes
}

context {defdir (yes){
		$(hello) : include
		${which} : bin
		\{
	}
	defdir(		 ) # This is a comment
	{
		nonono}
	undefdir (hello)
	raw  :whatsup
	where
	make{
		anything,
		here.
		\}
	}
	context {
		dosomething
		here
		defdir (no){
			hhh : yyy
		}
	}
}

context{defdir(no){.*:he}defdir(yes){nono}undefdir(yes)}
\}
\}
}
